import { AlarmIcon } from "../vectors";
import * as Styled from "./AlarmStyled";

const Alarm = () => {
  return (
    <Styled.AlarmStyled>
      <AlarmIcon />
    </Styled.AlarmStyled>
  );
};

export default Alarm;
